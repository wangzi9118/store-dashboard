# 门店财务运营看板 v1.0 部署说明

## 环境要求

- Linux 或 Windows 服务器
- Node.js 22.5 或更高版本，推荐 Node.js 24
- 对外域名与 HTTPS 反向代理（推荐 Nginx 或 Caddy）

## 启动

解压发布包后，在目录中运行：

```bash
npm start
```

服务默认监听 `0.0.0.0:5174`。可通过环境变量修改：

```bash
HOST=127.0.0.1 PORT=5174 npm start
```

生产环境建议让 Node 服务只监听 `127.0.0.1`，再由 Nginx 或 Caddy 转发公网 HTTPS 请求。

## 数据目录

SQLite 数据库会自动创建在 `data/store-dashboard.sqlite`。升级或重新部署时不要删除 `data/`，并定期备份该目录。

## 持续运行

使用 systemd 或 PM2 托管 `npm start`，确保服务器重启后自动恢复服务。

## 登录与权限

系统使用服务端 SQLite 保存账号、会话和门店查看权限，登录状态通过 HttpOnly Cookie 维护。

首次启动会自动创建管理员账号：

- 账号：`wyb`
- 密码：`123456`

管理员登录后可创建观察者账号、删除观察者，并为每个观察者勾选可查看的门店。观察者只能查看已授权门店的数据看板和门店 PK，不能进入月度录入、门店管理或权限管理，也不能通过接口写入数据。

首次登录后请在服务器侧做好访问控制，并通过 HTTPS 反向代理对外提供服务。数据库文件位于 `data/store-dashboard.sqlite`，升级或重新部署时不要删除该文件，并定期备份。
