# 门店财务运营看板 v2.0 部署说明

## 环境要求

- Linux 或 Windows 服务器
- Node.js 22.5 或更高版本，推荐 Node.js 24
- 对外域名与 HTTPS 反向代理（推荐 Nginx 或 Caddy）

## 启动

解压发布包后，在目录中运行：

```bash
npm start
```

服务默认监听 `0.0.0.0:5174`。可通过环境变量修改：

```bash
HOST=127.0.0.1 PORT=5174 npm start
```

生产环境建议让 Node 服务只监听 `127.0.0.1`，再由 Nginx 或 Caddy 转发公网 HTTPS 请求。

## MySQL 数据库

系统使用 MySQL 保存账号、权限、会话和全部业务数据。启动时会自动创建数据表。请先创建数据库，并在部署环境配置 `.env.example` 中列出的 `MYSQL_*` 环境变量。

微信云托管重新构建或扩容时，各实例连接同一个 MySQL 数据库，因此发布不会清空账号或门店数据。浏览器本地缓存不会自动回写到空数据库。

## 持续运行

使用 systemd 或 PM2 托管 `npm start`，确保服务器重启后自动恢复服务。

## 登录与权限

系统使用 MySQL 保存账号、会话和门店查看权限，登录状态通过 HttpOnly Cookie 维护。

首次启动会自动创建管理员账号：

- 账号：`wyb`
- 密码：环境变量 `DEFAULT_ADMIN_PASSWORD` 的值；未配置时才回退为 `123456`

管理员登录后可创建观察者账号、删除观察者，并为每个观察者勾选可查看的门店。观察者只能查看已授权门店的数据看板和门店 PK，不能进入月度录入、门店管理或权限管理，也不能通过接口写入数据。

首次登录后请立即修改密码，并通过 HTTPS 对外提供服务。请在 MySQL 服务侧配置自动备份。
